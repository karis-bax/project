{
  "name": "Envelope",
  "install": "cd backend && uv sync && cd ../frontend && npm install",
  "terminals": [
    {
      "name": "api",
      "command": "cd backend && uv run uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload",
      "description": "FastAPI backend on port 8000"
    },
    {
      "name": "web",
      "command": "cd frontend && npm run dev -- --host 0.0.0.0 --port 5173",
      "description": "Vite + React frontend on port 5173"
    }
  ],
  "ports": [
    { "name": "api", "port": 8000 },
    { "name": "web", "port": 5173 }
  ]
}
